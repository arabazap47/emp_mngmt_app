name: Build Employee App
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Build Docker Image
        run: docker build -t emp_mngmt_app .
      - name: Docker Images
        run: docker images
    